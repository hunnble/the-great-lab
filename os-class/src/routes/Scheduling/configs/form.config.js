export const jcbFormConfig = {
  form: 'jcb-form',
  fields: ['name', 'arriveTime', 'serviceTime', 'startAddress', 'memory', 'tapeDriveNum', 'priority']
}

const config = {
  jcbFormConfig
}

export default config
